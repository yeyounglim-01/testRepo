<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>슬립 웰니스</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;700&display=swap');

        :root {
            --bg-color: #0d1a2f;
            --text-color: #e0e6f3;
            --accent-color: #4c8a98;
            --highlight-color: #8bb3c2;
            --card-bg: #1a2a43;
            --shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
            position: relative;
        }

        .container {
            width: 90%;
            max-width: 450px;
            text-align: center;
            padding: 2.5rem;
            background: linear-gradient(145deg, var(--card-bg), #112238);
            border-radius: 20px;
            box-shadow: var(--shadow);
            position: relative;
            z-index: 2;
            backdrop-filter: blur(10px);
        }

        h1 {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--highlight-color);
            margin-bottom: 0.5rem;
        }

        p.subtitle {
            font-size: 1rem;
            color: var(--text-color);
            opacity: 0.8;
            margin-bottom: 2rem;
        }

        .sleep-status {
            background-color: var(--accent-color);
            border-radius: 50%;
            width: 250px;
            height: 250px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            margin: 0 auto 2rem;
            position: relative;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(76, 138, 152, 0.5);
            animation: pulse 3s infinite ease-in-out;
        }

        .sleep-status::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            clip-path: circle(50% at 50% 50%);
            animation: ripple 6s infinite ease-out;
        }

        .status-text {
            font-size: 1.2rem;
            font-weight: 400;
            opacity: 0.9;
        }

        .status-time {
            font-size: 3rem;
            font-weight: 700;
            line-height: 1.2;
            margin-top: 0.5rem;
        }

        .sleep-info {
            display: flex;
            justify-content: space-around;
            gap: 1rem;
            margin-bottom: 2.5rem;
        }

        .info-card {
            background-color: var(--card-bg);
            padding: 1rem;
            border-radius: 12px;
            flex: 1;
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }

        .info-card:hover {
            transform: translateY(-5px);
        }

        .info-card h3 {
            font-size: 1rem;
            font-weight: 400;
            color: var(--highlight-color);
            margin-bottom: 0.5rem;
        }

        .info-card p {
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0;
        }

        .button-group {
            display: flex;
            justify-content: space-between;
            gap: 1rem;
        }

        .cta-button {
            padding: 1rem 1.5rem;
            border: none;
            border-radius: 15px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
            flex: 1;
        }

        .cta-button.primary {
            background-color: var(--highlight-color);
            color: var(--bg-color);
        }

        .cta-button.secondary {
            background-color: var(--card-bg);
            color: var(--highlight-color);
            border: 2px solid var(--highlight-color);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            opacity: 0.9;
        }

        .background-stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
            z-index: 1;
        }

        .star {
            position: absolute;
            background-color: #fff;
            border-radius: 50%;
            opacity: 0;
            animation: twinkle 5s infinite ease-in-out;
        }

        /* Star animation setup - dynamically generated with JS in a real app */
        .star:nth-child(1) { top: 10%; left: 20%; width: 2px; height: 2px; animation-delay: 0s; }
        .star:nth-child(2) { top: 30%; left: 70%; width: 1px; height: 1px; animation-delay: 1.5s; }
        .star:nth-child(3) { top: 50%; left: 40%; width: 3px; height: 3px; animation-delay: 3s; }
        /* Add more stars as needed */

        /* Animations */
        @keyframes pulse {
            0% { transform: scale(1); box-shadow: 0 0 10px rgba(76, 138, 152, 0.4); }
            50% { transform: scale(1.05); box-shadow: 0 0 25px rgba(76, 138, 152, 0.7); }
            100% { transform: scale(1); box-shadow: 0 0 10px rgba(76, 138, 152, 0.4); }
        }

        @keyframes ripple {
            0% { transform: scale(0); opacity: 0; }
            50% { transform: scale(1); opacity: 1; }
            100% { transform: scale(1.5); opacity: 0; }
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0; transform: scale(0.5); }
            50% { opacity: 1; transform: scale(1.2); }
        }
    </style>
</head>
<body>
    <div class="background-stars">
        <div class="star"></div>
        <div class="star"></div>
        <div class="star"></div>
    </div>
    <div class="container">
        <h1>슬립 웰니스</h1>
        <p class="subtitle">당신의 편안한 밤을 위한 여정</p>

        <div class="sleep-status">
            <span class="status-text">오늘 밤, 당신의 수면은...</span>
            <span class="status-time">7시간 4
